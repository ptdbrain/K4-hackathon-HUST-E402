# Reflection — Phạm Danh Tuấn Dũng · 2A202601978

- Vai trò: Frontend, demo path và fallback.
- Phần mang tên tôi: flow 4 bước, nhãn AI thật/offline mock, repo demo, finding rủi ro cao nhất, “AI đánh giá sai” và rerun.
- AI hỗ trợ: dựng bản nháp UI và chạy browser automation; quyết định thông tin nào phải hiện để user biết giới hạn cần người phụ trách xác nhận.
- Nội dung trọng tâm: dry run Playwright đã đi hết Codelab mẫu → xác nhận requirement → repo demo → `NOT READY`; footer luôn nói hệ thống không sửa, chạy hoặc nộp bài.
- Bài học UX: khi backend không chạy, UI từng lộ lỗi `JSON.parse`; parser response nay trả thông báo lỗi chung thay vì chi tiết kỹ thuật.
- Lần sau tôi sẽ test đường lỗi trước happy path và hiển thị thời gian ước tính cho bước AI có thể mất gần 90 giây.
